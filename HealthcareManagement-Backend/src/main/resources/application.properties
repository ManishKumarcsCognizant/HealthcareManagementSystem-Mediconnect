server.port = 8081

# --- FIXED DATABASE CONNECTION ---
# Simplified SSL parameters leveraging Java 21's native truststore
spring.datasource.url = jdbc:mysql://mysql-8f270fb-cognizant-9905.g.aivencloud.com:20561/defaultdb?sslMode=REQUIRED
spring.datasource.username = avnadmin
spring.datasource.password = ${AIVEN_DB_PASSWORD}
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# --- JPA & HIBERNATE SETTINGS ---
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update

# --- SECURITY FILTER WARNING ---
# Note: In Spring Boot 3.x (indicated by Tomcat 10 and Hibernate 6 in your log),
# 'security.basic.enable' and 'security.ignored' are completely ignored and do nothing.
# If you run into 401/403 Unauthorized errors on your endpoints later, you will need
# to permit access via a Java SecurityFilterChain configuration class.

# Enable debug logs for authentication filter troubleshooting
logging.level.com.application.filter=DEBUG