server.port = 8081

# Corrected JDBC URL (Added jdbc:, removed inline credentials, and fixed SSL params)

spring.datasource.url = jdbc:mysql://mysql-8f270fb-cognizant-9905.g.aivencloud.com:20561/defaultdb?useSSL=true&sslMode=REQUIRED&trustCertificateKeyStoreUrl=file:src/main/resources/ca.pem&trustCertificateKeyStoreType=PEM
spring.datasource.username = avnadmin
# Fixed complete password

spring.datasource.password = ${AIVEN_DB_PASSWORD}
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver

# JPA & Hibernate Settings
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

# Security Settings
security.basic.enable = false
security.ignored = /**